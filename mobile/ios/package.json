{
  "name": "hotportion-mobile",
  "version": "1.0.0",
  "description": "Hot Portion Grill - iOS Capacitor Build",
  "scripts": {
    "ios:add": "npx cap add ios",
    "ios:sync": "npx cap sync ios",
    "ios:build-release": "cd ios/App && xcodebuild -workspace App.xcworkspace -scheme App -configuration Release -archivePath App.xcarchive archive"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0",
    "@capacitor/ios": "^5.7.0"
  },
  "dependencies": {
    "@capacitor/core": "^5.7.0",
    "@capacitor/splash-screen": "^5.0.7",
    "@capacitor/status-bar": "^5.0.7"
  }
}
